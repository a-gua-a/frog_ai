<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.nuist.frog_ai.server.mapper.MessageDetailMapper">
    <!-- 新增消息详情 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
        insert into message_detail(message_id, content)
        VALUES (#{messageId}, #{content})
    </insert>

    <select id="getDetail" resultType="cn.nuist.frog_ai.pojo.entity.MessageDetail">
        select * from message_detail where message_id = #{messageId} and content = #{conversationId}
    </select>
</mapper>